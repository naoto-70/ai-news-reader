# Codex 作業ガイド

このリポジトリは、2026-09-20 から Codex を主な開発・保守アシスタントとして管理する。
ユーザーへの説明、ニュース本文、運用ドキュメントは日本語を基本とする。

## 構成

- `docs/index.html`: 最新号。CSS を内包する静的 HTML。
- `docs/archive/YYYY-MM-DD.html`: 過去号。
- `.github/workflows/deploy.yml`: `main` への push で `docs/` を GitHub Pages に公開する。
- ビルド処理、パッケージ依存、ニュース収集スクリプトは現時点ではない。

## ニュース更新

### 選定方針

- AIは技術寄りの記事を優先する。モデルのアーキテクチャ・学習・推論、API、SDK、開発ツール、エージェント実装、評価手法、オープンソース、性能・レイテンシ・コスト、技術的なセキュリティ対策を主な対象とする。
- AIの経営・資金調達・一般的な政策や提携の話題は優先度を下げ、開発や運用への具体的な影響がある場合に採用する。要約では「何が変わったか」「技術的な仕組み」「実装・運用への影響」を、出典で確認できた範囲で説明する。提供元の性能主張と独立した評価は区別する。
- 新たに「決済」カテゴリを設け、記事カードのタグを `決済` とする。AIとの関連は必須ではない。
- 決済の優先順位は、(1) CAFIS本体とADAPTISなど関連サービスの変更、(2) Visa・Mastercard・JCBなど国際ブランドのルール、認証、トークン、精算や不正対策、(3) GMO-PGなど国内PSP・決済センター・カード会社・加盟店接続・端末の変更、(4) EMVCo・PCI SSCなど共通仕様・基準の変更とする。CAFISの名称を含む発表だけに絞らず、国内決済基盤の運用担当者が把握すべき変更を選ぶ。
- 決済の記事では「CAFIS/ADAPTISとの直接の関係が出典で確認できる」「業界共通ルール・接続先・競合など関連動向」「国外・別レールで参考になる動向」を区別する。後二者をCAFISへの採用・接続・影響が確定したかのように書かず、どの運用・実装判断に関係しうるか具体的に示す。消費者向けポイント還元・キャンペーンや関連の薄い暗号資産は優先しない。
- 決済はNTT DATA / CAFIS、Visa、Mastercard、JCB、GMO-PG、その他の決済事業者、EMVCo、PCI SSC等の公式発表・公開技術資料・開発者向け変更履歴を横断して一次情報を確認する。公開されていない接続仕様を推測で補わない。
- AIは直近24〜48時間、決済は通常直近14日を中心に調べる。実施・適用がこれからの重要な変更や見落としていたCAFIS関連の発表は30日程度までさかのぼってよい。公開日と発効日を分けて記載し、既掲載の話題を日を改めて使い回さない。
- 1号はAI技術5件、決済5件の合計10件を基本とする（各カテゴリ最大5件）。十分に探しても検証できる情報が不足するカテゴリは件数を減らし、他カテゴリで穴埋めしない。件数のために同じ変更を細分化したり、古い発表を再掲したりしない。

### 更新手順

1. 更新対象日を日本時間（Asia/Tokyo）で確認する。
2. 必ず Web で記事を確認する。公式発表などの一次情報を優先し、記事の公開日と出来事の日付を区別する。未確認の数値・製品名・URLを作らない。
3. 日付が変わる場合は、現在の `docs/index.html` をその掲載日のアーカイブに保存する。アーカイブ側の相対リンクを調整し、既存の過去号を不用意に上書きしない。
4. 最新号の日付（title、ヘッダー、ナビゲーション）と記事カードを更新する。通常はAI技術5件と決済5件の合計10件とし、検証できる記事が足りない場合は件数を優先して埋めない。
5. `CARDS:START/END` と `ARCHIVE-LINKS:START/END` のコメントを保ち、直近の過去号へのリンクを更新する。
6. 出典名と記事に直接アクセスできる URL を付ける。HTML の特殊文字をエスケープし、別タブのリンクには `rel="noopener"` を付ける。

## 変更と確認

- 既存のレイアウト、モバイル表示、ダークモードを引き継ぐ。
- ニュース本文中の Claude / Anthropic は報道対象名であり、管理移行を理由に置換しない。
- 変更前に Git の状態と差分を確認し、ユーザーの作業を上書きしない。
- HTML を変更したら、日付、記事数、ローカルリンク先、ブラウザー表示を確認する。ドキュメントだけの変更は差分確認でよい。
- `main` への push は公開処理を起動する。実施した変更、確認内容、公開の成否を区別して報告する。
- 秘密情報や認証トークンをファイルに記録しない。
- 定期実行は別途設定が必要。設定・確認していない自動更新を稼働中と説明しない。
